# Main GraphQL Schema for MicroPlanner

# Base types (extended in other files)
type Query {
  _empty: String
}

type Mutation {
  _empty: String
}

type Subscription {
  _empty: String
}

# User type, SubscriptionTier enum, and me/user queries live in user.graphql
# (defining them here too made the merged SDL invalid — duplicate types).
